# Workflow Step: Sync Entry Points

Keep root-level agent entrypoints aligned with `.dionagent/`.
The compatibility surfaces must be lightweight bridges only.
The real operating content lives under `.dionagent/`.
