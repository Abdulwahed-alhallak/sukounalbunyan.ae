# Workflow Step: Classify Stack

## Goal
Convert discovery evidence into a reliable stack classification.

## Default logic
When repository evidence is partial or ambiguous, use the following defaults:
- backend default: Laravel
- database default: MySQL on XAMPP locally
- frontend default: multilingual React + Vite or Next.js
- git/cicd default: GitHub + GitHub Actions
- production default: Hostinger
