# Workflow Step: Decide Strategy

## Decision tree
- If repository is empty or skeletal, choose greenfield mode.
- If repository contains meaningful code, routes, build files, migrations, or deployment logic, choose brownfield mode.
- If repository contains both backend and frontend with separate runtimes, choose split-delivery planning.
